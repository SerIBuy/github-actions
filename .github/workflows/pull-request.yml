name: Print Pull Request
on: workflow_dispatch
jobs:
    print:
        runs-on: ubuntu-latest
        steps:
            - name: Print context
              run: echo "${{toJson(github)}}" 
        
